//ADD INTO SHAPE

void update2(){
    for (int y=0; y<arr.length; y++) {
      int value = arr[y][0];
      arr[y][0] = value + 1;
      }
  }
  
   void update3(){
    for (int y=0; y<arr.length; y++) {
      int value = arr[y][0];
      arr[y][0] = value - 1;
      }
  }
